import { createStorage, StorageType, type BaseStorage, SessionAccessLevel } from './base';
import { exampleThemeStorage } from './exampleThemeStorage';
import { codeOptionStorage } from './codeOptionStorage';
import { preferencesStorage } from './preferencesStorage';

export { preferencesStorage, codeOptionStorage, exampleThemeStorage, createStorage, StorageType, SessionAccessLevel, BaseStorage };
